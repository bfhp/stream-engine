<div class="d-flex ms-lg-auto order-2 order-lg-4">
    <button type="button"
            class="btn btn-outline-success"
            data-bs-toggle="modal"
            data-bs-target="#authFormModal">
        {{ trans('view.auth.sign_in') }} <span class="d-sm-none">{{ trans('view.auth.sign_in_or_register') }}</span>
    </button>
</div>
