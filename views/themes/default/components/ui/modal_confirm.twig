<!-- Global Confirm Modal -->
<div class="modal fade" id="globalConfirmModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="globalConfirmTitle">{{ trans('view.ui.confirmation') }}</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="globalConfirmBody">
                {{ trans('view.ui.are_you_sure') }}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    {{ trans('view.comments.cancel') }}
                </button>
                <button type="button" class="btn btn-danger" id="globalConfirmOk">
                    {{ trans('view.ui.confirm') }}
                </button>
            </div>
        </div>
    </div>
</div>
